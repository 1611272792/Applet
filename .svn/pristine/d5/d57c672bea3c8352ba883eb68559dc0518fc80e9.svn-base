<!--pages/rhjxjjsq/rhjxjjsq.wxml-->
<view>
  <view class="tuihead myfont">
    如何进行奖金授权?
  </view>
</view>
<view class="tui-card">
  <view class="tuicontent">
    1.通过点击底部导航栏的【功能】按钮,进入功能界面->点击【奖金授权管理】（只有奖金项负责人才有权力进入此页面对自己所负责的奖金项进行授权）进入页面后可以看到自己可以授权的奖金项列表
  </view>
  <view class="tui-card-content">
    <swiper class="tui-swiper" indicator-dots="{{indicatorDots}}" vertical="{{vertical}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}" indicator-color="#777" indicator-active-color="red">
      <block wx:for-items="{{imgUrls}}">
        <swiper-item>
          <image class="tui-img" src="{{item}}" mode='scaleToFill' />
        </swiper-item>
      </block>
    </swiper>
  </view>
</view>
<view class="tui-card">
  <view class="tuicontent">
    2.左滑点击【添加授权】可以对此奖金项进行授权
  </view>
  <view class="tui-card-content">
    <swiper class="tui-swiper" indicator-dots="{{indicatorDots}}" vertical="{{vertical}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}" indicator-color="#777" indicator-active-color="red">
      <block wx:for-items="{{imgUrls2}}">
        <swiper-item>
          <image class="tui-img" src="{{item}}" mode='scaleToFill' />
        </swiper-item>
      </block>
    </swiper>
  </view>
</view>
<view class="tui-card">
  <view class="tuicontent">
    3.通过点击奖金项列表可以看到对应的授权记录，然后点击对应的被授权人可以查询详细的授权记录；列表默认显示当月授权记录，也可以通过时间筛选其他时间段的记录，点击每一行可以看到备注信息；同时奖金负责人也可以收回对应的奖金授权
  </view>
  <view class="tui-card-content">
    <swiper class="tui-swiper" indicator-dots="{{indicatorDots}}" vertical="{{vertical}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}" indicator-color="#777" indicator-active-color="red">
      <block wx:for-items="{{imgUrls3}}">
        <swiper-item>
          <image class="tui-img" src="{{item}}" mode='scaleToFill' />
        </swiper-item>
      </block>
    </swiper>
  </view>
</view>

