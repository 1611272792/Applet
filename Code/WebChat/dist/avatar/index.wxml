<view class="i-class i-avatar i-avatar-{{ shape }} i-avatar-{{ size }} {{ src ? 'i-avatar-image' : '' }}">
  <view class='abc' wx:if="{{ src !== '' }}">
    <image src="{{ src }}"></image>
  </view>

  <view class="i-avatar-string" wx:else>
    <slot></slot>
  </view>
</view>